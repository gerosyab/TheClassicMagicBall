<?xml version="1.0" encoding="utf-8"?>
<!--
  Title: width-driven — image is 85% of parent width; height = intrinsic (aspect ratio).
  Row height is not a fixed dp; it follows the drawable at 85% width.
-->
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:id="@+id/title_banner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="false"
        android:focusable="false"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="@dimen/title_side_weight" />

        <ImageView
            android:id="@+id/title_image"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="@dimen/title_image_weight"
            android:adjustViewBounds="true"
            android:contentDescription="@string/app_name"
            android:scaleType="fitCenter"
            android:src="@drawable/title" />

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="@dimen/title_side_weight" />
    </LinearLayout>
</merge>
